<h1>Songs#show</h1>
<% @partitions.each do |partition| %>
  <ul>
  <li><%= partition.title %><%= link_to 'SEE PARTITION', song_partition_path(partition.song_id, partition.id) %></li>
  </ul>
<% end %>
